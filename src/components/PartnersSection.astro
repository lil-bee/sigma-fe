---
import type { PartnerLogo } from "../interfaces/partner";
import fetchApi from "../lib/strapi";

const partners = await fetchApi<PartnerLogo[]>({
  endpoint: "partners-logos?populate=logos",
  wrappedByKey: "data"
});
---

<section id="industries" class="flex flex-col items-center justify-center gap-6 md:gap-9 p-6 md:p-16 w-full bg-white">
  <h2 class="text-lpblack text-2xl md:text-3xl lg:text-[40px] leading-tight md:leading-[60px] font-['Plus_Jakarta_Sans'] font-bold text-center">
    Trusted by renowned companies worldwide
  </h2>

  <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-6 max-w-full w-full">
    {partners.map((partner) => (
      <img
        src={import.meta.env.STRAPI_URL + partner.logos.url}
        alt={partner.logos.alternativeText ?? partner.company_name}
        class="h-10 md:h-14 w-auto mx-auto"
        loading="lazy"
      />
    ))}
  </div>

  <a
    href="/partners"
    class="bg-[#e53838] hover:bg-[#e53838]/90 text-lpgreylight rounded-xl px-4 md:px-6 py-2 md:py-3 flex items-center justify-center gap-2"
  >
    <span class="font-['Plus_Jakarta_Sans'] font-semibold text-sm md:text-base leading-4">
      View All
    </span>
    <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
    </svg>
  </a>
</section>
